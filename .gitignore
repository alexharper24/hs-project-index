# The INTERNAL page. Carries grades, open-item counts, uncommitted flags, and
# local filesystem paths, so it must never reach the public repo.
# Regenerate with `python build.py`.
index.html

__pycache__/
