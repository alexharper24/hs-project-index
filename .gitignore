# Generated. Regenerate with `python build.py`.
# Kept out of git so an accidental push carries no client data: the page
# embeds client names, draft-site status, and local filesystem paths.
index.html

# Captured screenshots of client sites. Regenerate with `python capture.py`.
img/tiles/
__pycache__/
